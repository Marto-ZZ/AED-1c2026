\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}
\usepackage{amsmath, amssymb, amsthm}
\usepackage[margin=2.5cm, headheight=15pt]{geometry}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage[most]{tcolorbox}
\usepackage{enumitem}
\usepackage{multicol}
\usepackage{booktabs}
\usepackage{parskip}

% Colores
\definecolor{algoblue}{RGB}{0, 82, 155}
\definecolor{tipbg}{RGB}{245, 245, 250}
\definecolor{tipframe}{RGB}{200, 200, 220}

% --- ENCABEZADOS Y PIES DE PÁGINA ---
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\textbf{Algoritmos y Estructuras de Datos}}
\fancyhead[R]{\textbf{Guía 1}}
\fancyfoot[L]{\mensajepie} % <--- Acá se inyecta el mensaje random automáticamente
\fancyfoot[C]{\thepage}
\fancyfoot[R]{\hyperlink{indice}{\color{algoblue}Ir al índice $\uparrow$}}

% Macros
\newcommand{\mensajepie}{%
    \pgfmathparse{int(random(1,3))}% Genera un número del 1 al 3
    \ifcase\pgfmathresult\relax%
    \or \textcolor{gray}{\scriptsize $\star$ ¿Te sirvió el apunte? ¡Dejale una estrellita al repo!}% Opción 1
    \or \textcolor{gray}{\scriptsize !` ¿Encontraste un error? ¡Mandá un PR o abrí un issue!}% Opción 2
    \or \textcolor{gray}{\scriptsize $\langle / \rangle$ ¡Aportá tus resoluciones! Todo sirve para mejorar.}% Opción 3
    \fi
}

\newtcolorbox{disclaimerbox}{
    colback=tipbg,
    colframe=tipframe,
    fonttitle=\bfseries,
    title=¡Recomendación para sacarle jugo al apunte!,
    arc=3mm, boxrule=1pt, left=10pt, right=10pt, top=10pt, bottom=10pt
}
